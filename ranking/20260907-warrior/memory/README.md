# 本次通关使用的项目 memory 摘录

审计范围为战士通关主会话 `01a06f98-2e8a-7f22-9e05-4d33211253cc` 及其 8 个子／孙会话，覆盖 2026-09-05 至 2026-09-07 的通关过程和后续归档操作。以实际工具调用、解析器导入链和系统注入内容为依据，区分读取、执行、修改和仅被提及。

## 文件清单

来源根目录：`~/.codex/memories/`。本目录保留来源相对路径；索引和摘要已整理为本项目摘录，解析器保持原内容。

| 归档文件 | 当时的使用方式 | 本次代理是否修改原文件 |
|---|---|---|
| [MEMORY.md](MEMORY.md)（项目摘录） | 主代理和多个子代理用 `rg`、`sed`、`nl` 搜索或读取游戏相关段落，获取存档检查、操作顺序和历史机制信息 | 否 |
| [只读存档解析器](extensions/ad_hoc/notes/20260824T193329+0800-spd-v338-readonly-inspector.md) | 用 `sed`／`rg` 阅读实现；直接作为 Python 程序执行 `slots`、`status`、`map`、`entities`、`route`；随后由 `checkpoint.py` 的 `SourceFileLoader` 反复导入，`yog-lines.py` 通过 `checkpoint` 间接使用 | 否 |
| [memory_summary.md](memory_summary.md)（项目摘录） | 摘要由系统注入会话上下文并被使用；与上面两项不同，通关时没有工具主动打开该磁盘文件 | 否 |

因此，有明确工具读取／执行记录的来源文件为 2 个，另有 1 个系统注入摘要来源。本目录提供 **2 份项目摘录、1 份完整的项目解析器和此清单**。通用个人信息和其他项目内容均不收录。

## 读取与修改的区别

核对实际操作后，没有发现本次主代理或子代理在原 memory 目录内创建、修改或删除文件的操作。本次的脚本、快照、战术笔记写入原专用临时目录；后续归档写入项目的 `ranking/20260907-warrior/`。保存解析器副本的复制操作读取了原 memory 文件，目标为项目目录。

`checkpoint.py` 从创建时就设置 `sys.dont_write_bytecode = True`，再通过 `SourceFileLoader` 导入解析器；直接运行解析器 `.md` 文件是将其作为主程序执行。清点时解析器同级也没有 `__pycache__` 目录。

`MEMORY.md` 中列出的其他历史笔记、`rollout_summaries/`、`extensions/skysight/resources/` 等文件，虽然路径出现在索引或系统摘要中，但本次没有发现直接读取其正文的工具记录，所以没有把“索引提及”算成“实际读取”并整批复制。

## 代表性会话证据

以下是本机 Codex JSONL 会话日志的文件名和行号，仅作为操作证据索引；会话日志本身没有复制到此目录。

| 操作 | 日志与行号 |
|---|---|
| 主代理首次检索 `MEMORY.md` | `rollout-2026-09-05T11-23-59-01a06f98-2e8a-7f22-9e05-4d33211253cc.jsonl:13` |
| 主代理读取索引段落 | 同一主会话日志 `:24` |
| 主代理首次读取解析器源码 | 同一主会话日志 `:42` |
| 主代理直接执行解析器 `slots` | 同一主会话日志 `:54` |
| 存档助手创建导入解析器的 `checkpoint.py`，写入 `/tmp` | `rollout-2026-09-05T11-25-47-01a06f99-d536-71d1-85a1-5dc5c6168cf0.jsonl:50` |
| 战术助手读取 `MEMORY.md` | `rollout-2026-09-05T11-24-33-01a06f98-b379-78f3-ba77-73af5058f622.jsonl:17`、`:23` |
| 洞穴怪物助手读取索引 | `rollout-2026-09-06T11-29-02-01a074c3-2855-7570-8a67-3bb2971ece54.jsonl:29`、`:42` |
| 尤格拳头助手读取索引 | `rollout-2026-09-06T23-33-12-01a0775a-28ea-7f62-84b0-9930718b504a.jsonl:71`、`:77` |

## 公开归档范围与版本

索引保留五类项目内容：战士通关协议、macOS 存档路径、项目 fork 与 CLI 控制设计、桌面构建与打包、源码机制。摘要只保留本项目的使用偏好及历史条目；个人画像、其他项目和独立于本游戏的系统工具／录屏记录均已移除。项目根目录以 `<project-root>` 表示。

来源为归档时的磁盘版本，经过项目范围筛选；此处不保存完整个人记忆或每一次历史读取版本。全局记忆目录中的原文件保持原状。

只读解析器统一保留在本目录的 `extensions/ad_hoc/notes/` 下，归档中只有这一份。历史临时脚本与笔记整理在 [../tmp/](../tmp/)，其内容及通关时的路径引用保持原样。

本次整理仅作用于项目内归档，没有写入或更新 `~/.codex/memories/`。
