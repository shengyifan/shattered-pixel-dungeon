# 2026-09-12 为新实战清空本地生成数据

> 文档整理说明：配套的历史验收 JSON 已按用户要求删除；原始运行数据也已清空。本页保留当时的验证说明，旧结构化结果可从 Git 历史查阅，不能作为 CLI.0.9.0 的新验收结果。

用户在了解audit与策略轨迹作用后，明确要求将这些未纳入Git的数据全部移除，准备后续重新打包CLI.0.9.0进行实战。本次清理取代当日较早“保留旧战士profile和当前包”的保留决定。

已停止本次构建使用的Gradle daemon，并删除全部14个Git忽略的生成目录/文件，删除前分配空间合计约3.31 GiB。包括所有模块build、本仓库.gradle、Python缓存、Finder元数据，以及由ios/build.gradle自动生成的robovm.properties。

因此，旧战士game1存档、audit双库、策略轨迹、全部本轮/历史测试profile、冻结runtime、0.8.12应用包与测试副本均已删除，旧请求账本及策略记忆不再可恢复或查询。没有将数据转移到别处留下备份。

保留.git、Git已跟踪的源码/配置/文档/报告，以及用户原有.gitignore修改；没有清理用户Library/Application Support或全局Gradle/Java缓存。删除前检查候选不包含任何已跟踪文件，删除后Git忽略清单为空，.gitignore内容与清理前完全一致。

这是数据与产物重置，源码版本仍是CLI.0.8.12；CLI.0.9.0尚未改版、打包或启动实战。后续实战需创建新的profile，从新局开始，不能再按已删除的旧路径继续。原验收报告仍是历史事实，但其中build路径不再代表保留的原始数据。

完整删除目录和体积见重置记录（历史 JSON 已删除，可查 Git 历史）。
