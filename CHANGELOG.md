# CLI 变更记录

版本格式为 `CLI.主版本.次版本.修订版本`。游戏版本与协议版本独立记录。

## CLI.0.8.12

- 按用户要求移除docs中的43份历史JSON报告/索引；静态UI测试基线原样迁至game-control/src/test/resources，并同步生成器、测试与职业矩阵。有效场景计划改为Markdown，旧链接改为历史说明；语料工具只接受显式当前trace，不再扫描已删除报告。5项Java清单检查与4项Python选择回归通过，基线内容和27/32/5项能力枚举未变；验证产生的本地构建缓存再次清理。

- 按用户后续明确要求，为计划中的CLI.0.9.0新实战清空剩余全部本地生成数据：旧战士存档、audit、策略轨迹、当前应用包、全部build/本仓库Gradle缓存等14项，约3.31 GiB。源码与报告保留，旧数据不再可续用；本次未改版或启动新实战。

- 当前ARM64实际应用包验收通过：包内JVM/SQLite、中文和空格路径、原始UTF-8/CRLF/末帧、去重/锁/EOF、原教程与库存取消/进食、成功保存回执和第二JVM同局恢复，公私库完整性和本地签名均正常。
- 按9月12日用户要求清理14份旧应用包及1420个旧测试目录/文件，原分配空间约19.54 GiB；当前测试结果、当前包和暂停战士的整个playthroughs保留。历史报告保留并明确旧原始数据路径已失效，不伪称仍能重建已删旧快照。

- 修复通用UI对未绘制或视口外浮字的全文泄漏：只读上一完整draw留下的可见word快照，在ID分配、节点创建和父文字聚合前过滤，不读取原全文兜底。部分word裁切不补齐，池复用与每帧清理旧缓存，未启用观察时不生成额外片段。
- 9月12日实际CLI连续pan外→内→外通过，原三浮字仍存活但隐藏时无文本/空节点/ID；15次中文窗口化核验。三模块测试任务共374项通过，61项相关定向覆盖与真实pan证据齐全，纠正旧Tengu只检查visual_cues的范围。

## CLI.0.8.11

- 占卜卷轴两项通过：有未知身份时原READ自身终态显示四个实际随机鉴定结果且耗一张；全已知时原无窗口提示、知识不变且仍耗一张。37次中文窗口化核验，初次标题大小写测试误判保留，同构建仅补失败项，未改随机结果。

- 首次支持提示按原完整双语资源组合和同一公开窗口树识别，仅真实Close按钮及子文字使用原Close语义；保留中文GUI额外说明与署名，不读取supportNagged或点击外链。6新增边界测试及24相关测试通过。
- 实际SewerBoss出口/WornKey与SkeletonKey原INSERT造锁/取消/开锁/重锁/DROP/强开两项完整通过。Worn拾钥真实经历支持提示Back保护与Close；原失败保留，不直接设置特殊锁地形。两项63响应/58GUI检查，容器与门累计12项347响应/279GUI检查。

## CLI.0.8.10

- 新增仅针对已完成测试runtime的离线失败诊断工具：从明确manifest枚举匹配profile，拒绝越界及各类符号链接，不扫描其他存档；优先选取翻译异常并标注选择策略，避免早期合法STALE_STATE遮蔽诊断。8项离线测试通过，输出仅留开发诊断。

- 支持原Mob.yell已显示的说话者、引号对白及尾空格组合；两端必须由完整已显示资源或模板安全翻译，未知说话者/正文/尾文与裁剪仍拒绝，不读NPC对象或旧消息补全。6项边界回归通过。
- 剩余制杖匠/幽灵装备召唤/幽灵攻击三项通过，126次中文窗口化核验。22个低频场景现在全部有通过证据（原14+确认5+对白3），642次选定GUI检查；各构建、旧失败和旧错误码保持独立，不冒称同一构建全量复跑。

## CLI.0.8.9

- 实际完整可见FloatingText增加公开presentation标记，仅复用现存纯可见性检查；依据已显示原词将战斗“闪避”与印记反馈译为dodged/marked for death。不调用defenseVerb或buff getter，不扩大文字/位置，6项正反例通过。
- P7英文矩阵188个具名场景全部已有通过证据：原冻结148/188加40项修复补验，保留每次原失败和构建身份，不冒称同一新版全量重跑；成功证据共4337条公开响应、3961次中文窗口化核验。
- 墓碑、铁门、水晶门第四组完整通过，墓碑实际trace含dodged浮字标记，确实触发并修复此前失败。容器累计10项、284条公开响应、221次GUI核验；Worn出口和万能钥匙特殊锁另组继续。

## CLI.0.8.8

- 静态完整能力/天赋正文与其原名称配对，只有当前窗口完整已显示说明匹配且结果唯一才消歧；短说明仍受原职业护甲菜单严格签名约束。六职业嵌套详情补齐为12个子职业、18个护甲能力和132个天赋说明，450次GUI核验，旧失败不覆盖。
- 新增独立纯公开对话签名类，区分偷窃、复活和奖励预览的原确认按钮；20项新增标题/物品/对话边界测试通过。六个确认补验中5完整通过，制杖匠已过确认但领取后对白仍失败，按原事实保留。
- 平衡符石武器/护甲两项完整通过：原Never mind、Back、应用及移除强化，取消不耗符石，两次应用恰耗两枚，已有强化选项不再广告。94次中文窗口化检查；仅测试初态添加三枚符石，普通战士数值不变。

## CLI.0.8.7

- 更新实施状态首页，区分旧版已通过矩阵、新英文严格首轮与独立补验；明确当前源码并非已验证的0.7.2应用包，后续重包验收保持未完成。

- 普通骨骸、英雄遗骸及待售物品取消/购买三项追加通过，容器累计7项、202条公开响应、155次中文窗口化核验。墓碑开出4幽灵后遭遇真实战斗浮字“闪避”歧义，整项仍失败；不把已完成局部计作完整通过。

- 22项低频矩阵完成英文CLI严格复跑，14通过/8失败；545次同版本中文窗口化核验，所有进程已清理。失败集中于偷窃/奖励/复活确认和幽灵对白，原报告保留，后续只补对应8项。测试断言改为操作自身终态，失败清理不追加UI决定。

- 子职业/护甲能力及嵌套天赋详情增加逐个原打开/Back实机验证；战士、法师、女猎、决斗者四职业完整通过，盗贼与牧师两个标题歧义仍失败。六项共413次中文窗口化核验，原四页6/6结果与新详情4/6范围分开。

- 修复连续未译显示覆盖原请求错误的问题：异常处理仍先持久化保存/结局回执，但不会再次翻译下一条日志而把文本故障误标为审计库不可用；原请求结果先按原错误类别持久化，后续错误照常私有记录并停止会话。
- 32项相关会话/回执/英文审计测试通过；新增真实双库回归同时验证PUBLIC_TEXT_UNAVAILABLE、REJECTED账本、唯一响应、两份完整私有原文与两条异常、不泄漏原文。实际物理审计失败处理和动作不确定性规则保留。

## CLI.0.8.6

- 锁箱与水晶箱两项追加通过：无钥匙拒绝、原钥匙拾取、逐箱消耗、打开与拾取，水晶法杖大类线索正向保留且具体隐藏载荷相互不泄漏。容器累计4项、130条公开响应、92次中文窗口化核验；其余类型和门仍pending。

- 依据当前完整已显示菜单为拳套系连击、暗影映像、升级返回和未鉴定卷轴取消确认提供原英文；不以隐藏装备/技能/已鉴定位推断界面。
- 补齐完整多段资源与已显示换行/空白的组合回归，包含Shockwave详情与Trinity原显示格式，保留原分隔符、数字和完整性检查。
- 124项定向回归通过；Gauntlet、ShadowClone、Upgrade、Cancel Confirm、Trinity五项真实专项通过，113次中文窗口化核验。原188项矩阵与独立补验分开记录，未覆盖的真实失败继续修复。

## CLI.0.8.5

- 角斗士与武僧招式列表按完整已显示名称、消耗格式和说明配对翻译，错误标题/消耗/尾文拒绝，不依赖隐藏招式对象。
- 暗影斗篷与匕首系“潜行”按钮仅根据当前物品详情根窗口、完整已知标题及原装备/放下/扔出菜单签名确定STEALTH或sneak；其他窗口与隐藏物品类型不能借用上下文。
- 新增10项边界测试、相关113项定向测试通过；盗贼原斗篷菜单、角斗士原技能、武僧原技能、匕首取消/施放4项补验通过，共82次中文窗口化检查。188项原矩阵仍保留原失败，补验不会覆盖基线。

## CLI.0.8.4

- 容器首组两项通过：三层物品堆按原顺序拾取与实际4回合消耗、双隐藏载荷普通宝箱的查看/原直接开启/拾取。57条公开响应与43次中文窗口化核验，开前内容不泄漏，开后装备等级仍未知；其余容器/锁门handler明确pending。

- 支持以已显示空格拼接的完整资源和参数模板，必须消费全部正文并产生唯一英文结果；保留原显示数字，不拆坏原多句模板或丢弃未知尾文。7项组合回归及WARDEN/冲击波实际取消与施放2项通过，44次中文窗口化核验。
- 根据公开完整说明页识别专精标题；胜利排行仅依据非模态排行场景、当前按钮及完整显示数字行选择原过去时文案，不读取记录中的胜负字段。4项边界回归通过。
- 六职业原说明窗口四页全部通过，126次同版本GUI核验；之前专精标题失败保留。详情按钮与嵌套天赋仍单独验证。
- 两项原护符结局流程通过，52次GUI核验：低层原拾取/留下/2→1/SURFACE/胜利恭喜Back限制/Close，以及25层原返程挑战取消/确认/24层稳定。均测试夹具，不计正式通关，不宣称26→1完整返程。

## CLI.0.8.3

- 测试客户端新增冻结运行产物复用；长动作只用原请求结果查询等待自身终态，不发送第二次动作或以新观察替代失败。8项离线runner回归通过，188项矩阵使用独立冻结副本持续执行。

- 六职业介绍的22个原生独立段落可单独翻译；只登记六条无参数资源中段数对应的完整段落，不恢复裁剪前缀、不接受未知尾文。
- 胜利关闭按钮仅按公开排行场景、模态、完整恭喜文字及Support/Close按钮签名输出Close，原Back限制保留。
- 54项相关单测通过。六职业实机均验证介绍/天赋页，完整四页专项仍在“专精”标题处失败；真实护符返程已验证恭喜窗/Back/Close，但后续胜利排行文字仍未译。保留这些明确缺口，当前不将两组整体算通过。

## CLI.0.8.2

- 将物品详情原正文采用的等级知识位关联到当前公开窗口，仅对确已展示的物品生效；容器不会带出隐藏内容。英文Spear说明依据公开知识位区分actual/typical，数值始终来自已显示文本。
- 目标提示仅以同一公开UI已有的完整英文cell_prompt消歧，不读取隐藏目标选择器、不补裁剪尾文。旧窗口、缺少知识位和未知完整文本仍明确拒绝。
- 验证：92项定向单测通过；6种物品详情/容器双世界场景及Spear正常、资源不足共8项真实CLI分支通过，206条公开响应、175次中文窗口化核验。完整职业矩阵继续独立复跑，失败批次原样保留。

## CLI.0.8.1

- 欢迎/更新场景补测4项通过：更新→Changes、补丁→Continue、未来版本警告，以及原无效临时文件清理的强制通知/禁用Continue/Back保护/原计时启用。仅构造隔离profile初态，用生产launcher走原UI。
- 键位配置补充原子窗/主窗Confirm、同动作重复键禁用、重新打开保持绑定及原Default Bindings恢复验证；只通过CLI的配置语义入口，不生成键盘事件。
- 原永久死亡/排行详情与死亡后重开两项通过，47个中文窗口化后置核验：无Ankh原Poison触发唯一lost，实际WndRanking完整内容可读；原Start New Game/Start创建健康新hero与新scope，旧死亡响应和事件不串局。额外误入Hero Info的未译支路单列pending，不假称已修复。
- 五个原笔记入口完整回归通过，563公开响应/279个同版本中文窗口化核验：文本、楼层、具体背包物品、公开物品类型及物品详情快捷入口，均原增改删/取消、保存和新JVM恢复。没有地图cell笔记功能，不虚构；取消前可能已分配物品关联ID的原行为保留。
- 用完整公开Note输入/查看/删除签名处理原确认与删除文案；不依据用户填入的标题猜测内部窗口，也不猜译用户未知正文。五类英文内容笔记的原增改删、取消、保存和重启专项已完成。
- 键位输入控件公开与既有ui.binding_key等价的binding_input标记。仅依据该可见控件、按钮父关系和完整显示模板区分Unbind Key按钮与None参数，不读取私有键码或捕获动作来解歧。
- 对应翻译器46项、投影15项、UI适配12项定向回归通过；设置实机补测F12原键位输入后Cancel，不改变原绑定，再验证主面板Back保护与原Cancel返回。确认保存、冲突与默认恢复分支继续独立测试。

## CLI.0.8.0

- 原开局校验补测通过：未来lastDaily profile的原警告（不改系统时钟），以及从公开存档详情取得普通局种子后输入同码的原冲突拒绝；不读取内部种子决定输入。
- 补充每日局已有存档拒绝、存档Erase取消/确认、原今日重玩说明与新scope的dailyReplay真实循环；只删除自建隔离测试档，不改时钟或直接操作存档文件。
- 原Randomize确认专项通过：选择2项随机挑战和随机角色后，原只读结果窗恰好显示2项，实际位图和已解锁角色后置断言一致；不为目标角色重抽或改RNG。
- 补充原种子Set→Start与Daily Run→Yes真实开局，通过实际新scope、一级战士、原种子/daily标记及正常保存退出断言；构造仅限此前profile进度，不直接调用初始化代替确认操作。
- 六项跨层专项完成英文CLI复跑，共96次中文窗口化后置核验：跌落原取消/确认和着陆效果、矿井往返、6/11/16/21层原区域故事及Continue。21层问题实证为测试清场初态的门未开放，原引擎正确纠正位置；仅修夹具，保留原超时、阶段和线程栈证据，不改生产规则。
- 增加已公开键位槽和父/动作关联的英文上下文，严格识别键位行与完整键位面板，修复选择快捷栏及确认按钮歧义；没有推断隐藏Window或Row类别。
- 支持完整对称游戏高亮标记中的已显示模板文字，保留原标记和可见数字，修复Journal的`_第1层_`显示；未知尾文及裁剪不补全。对应翻译器43项、投影9项、UI适配11项定向回归通过。
- 设置实机通过六页签、三个滑条原修改/恢复、键位面板Back保护与原Cancel、英文语言列表；真实GUI仍中文窗口化。原两项翻译失败和Back测试假设错误分别留档，没有把尚未编辑的按键和其余设置分支算通过。

## CLI.0.7.2

- 实际ARM64应用包英文验收通过：无source/test classpath或注入，受限系统PATH下实际加载包内JVM与SQLite；原首次教程、中文窗口化、英文库存操作/投掷取消/已画日志、原文event_sequence配对、正常保存重启世界与全库存一致，以及Unicode路径/原帧字节/去重/锁/EOF/签名均通过。原失败与测试定位修正分开留档。
- 新增开局选项中间状态测试：未胜利的3种原锁定说明、种子输入/长度拒绝/Set/Clear、挑战保存恢复、Randomize取消、Daily取消共7条英文CLI路径通过；GUI中文窗口化。未把未执行的真正开局和确认分支算通过。
- 修复实际应用包首次教程中“指南”的翻译歧义：仅根据已公开GameScene或AlchemyScene区分Guidebook与Guide，不根据隐藏物品或任意内部类型判断。完整词、未知场景、隐藏对象、未知尾文及裁剪边界回归通过，翻译器37项独立测试通过。
- 保留0.7.1包的实际失败记录；不通过跳过首次教程取得成功，使用新构建重新验证包内原流程。

## CLI.0.7.1

- 加强P7测试真实性：逐响应先留原trace再校验英文，逐版本后置核验中文窗口化，清理错误或非零退出都判失败。6项离线正反例通过；场景清单更新精确证据，仍不将partial条目计为全类通过。
- 公开UI增加`display.language`与`display.fullscreen`：分别读取真正选定的GUI语言及当前Gdx窗口模式，不受CLI临时英文读取上下文影响，便于直接验证中文窗口化。
- 已画英文日志的内部原文记录增加对应公开`event_sequence`，同一双库事务写入，避免只能依赖时间/内容推断关联。
- 语言/窗口状态回归、日志原文关联与事务失败回滚、既有生命周期检查通过。普通角色行为和存档格式不变；实际应用包验证继续进行。

## CLI.0.7.0

- 接通中文GUI、英文CLI：公开模型使用线程隔离的英文纯读取，缓存UI文字只依据已显示内容和公开控件上下文翻译。原游戏回调始终恢复GUI语言，保留原角色规则和RNG；未知完整文案明确失败，被裁片段不补隐藏尾文。
- `state/actions/action.execute/request.get/events/history`统一英文游戏文案；标识、路径和原始请求数据不改写。已显示日志的英文事件与完整原文在同一事务中分别保存公开事件和内部诊断，翻译异常也保留完整原始批次。
- 增加公开父控件/快捷键/滑条/保存详情等有证据的消歧；使用有限等义规范化处理Focus和可选句点，原55组资源碰撞目录保留。玩家自定义文字处理继续等待明确约定，不猜译为隐藏物品类型。
- 验证：两项严格实机用例通过新局原菜单/物品/投掷取消/进食已画日志/保存重启，以及真实旧中文审计副本的英文呈现；原两库完整行与快照块不变。六类主菜单与Journal四页签完成英文复跑，GUI仍中文窗口化。
- 对完全相同238个关闭fixture的5874条历史公开响应做纯JSON语料回归，从533次拒绝收敛至0拒绝、0partial、0跳过；不将这项结果称为238个当前引擎场景通过。完整Gradle套件通过，另有语言、显示词、公开上下文、双库原文与语料探针的独立回归。
- 场景覆盖继续依据显式清单补齐，正式战士仍保持已保存退出。实际0.7应用包另行验证；不冒用旧包结果，不自动推送或发布。

## CLI.0.6.0

- 语言解耦的首个独立改动：Messages增加仅当前线程生效且finally恢复的语言上下文，公开模型名称/说明按英文纯读取，原游戏动作保持中文环境。8项新增语言/线程/真实战斗RNG分支回归和既有29项模型测试通过；这一步不代表缓存UI文字、日志或旧历史已经全部英文化。
- 增加已公开文字的独立英文资源投影器：只读包内资源、递归处理已显示参数和完整句子，仅按已公开scene消除部分同字异译，无法安全翻译时明确拒绝；裁剪片段不补隐藏尾文。22项独立回归、4358无歧义完整资源与584模板样本检查通过，55组资源歧义和6个模板样本安全拒绝如实列为缺口；完整CLI集成继续实机验证。
- 建立170个场景/窗口条目及地图场景族的显式fixture覆盖清单，逐项保留partial/pending/N/A；静态可适配不冒充真实分支通过。
- 增加天狗第一阶段实际已上传图案的中立Tilemap观察与隐藏/淡出/跨回合更换测试；不读隐藏陷阱模型补足图案。增加5项实际跨层、支线和区域故事测试，21层故事仍因源准备未完成而pending。
- 主菜单6类原进入/返回流程与独立UI类断言通过；测试窗口标题标明“CLI 场景测试”，测试后关闭。首次欢迎、标题、选角以及6类目标场景均有真实类记录，不把未点过的按钮算已验证。
- 完整JUnit为191项通过；源码/测试编译与清单过期检查通过。
- 本批保存的是中文GUI和旧中文CLI文本基线。用户新增要求为中文GUI、全部英文CLI，语言分离优先实现并复跑；不改写旧审计，也不把本批报告算英文契约通过。正式实战继续保持停止。

## CLI.0.5.0

- 保留已暂停实战所用的公开协议客户端与离线策略回归；每次完整观察原子更新公开状态文件，未知提示保持连接。只作为可复查的开发工具，不声称已能自主通关，不自动重启正式局。
- 按用户最新要求停止正式战士实战，原局通过公开 CLI 成功保存退出；当前集中构造中间状态并由真实引擎执行全场景回归。场景清单的静态路线与实际通过证据分开，正式通关仍未完成，不以夹具胜利替代。
- 修复新 JVM 继续未祝福十字架待复活存档时窗口已显示但 CLI 永久等待 Actor 的问题：仅对死亡英雄且从未创建 Actor 线程的安全渲染边界开放观察，保留强制窗口、原选择/取消和保存规则。
- 增加原天狗炸弹实际烟雾、已画出的 3/2/1 字面量，以及矮人国王四种实际召唤粒子；不输出私有计时、召唤类型或 AI 阶段。共用粒子观察器保留真实尾粒子，首次等待与已显示证据分开，隐藏或暂停源不阻塞输入。
- 验证：187 项 JUnit 全通过；中文窗口化天狗可见/隐藏 2 项、粘咕尾粒子回归、矮人国王可见/隐藏 2 项、原生死亡/暂停保存/新 JVM 继续/选择取消/确认复活通过。原失败和测试布置问题保留具体证据，不伪造为全部场景已覆盖。
- 0.4 冻结 ARM64 包另外通过中文路径、内置 Java/SQLite、严格管道字节、实例锁、EOF、诊断归档与签名验证，结果目录为 `desktop-control/build/package-check/d659f0eff6884f3eba61126701f87025`；该结果不冒充 0.5 包验收。

## CLI.0.4.0

- 按最新要求将最终实战验收改为战士一局纯 CLI 通关，保留全角色功能和专项；新实机批次统一中文、窗口化，正式存档仅通过公开设置修改。旧英文批次保留原事实。
- 新增 game.log 显示快照事件，只记录真正画出的日志字词；不可见顶部、整条隐藏记录和 control ID 空洞不泄漏，控制台/原信号留在内部。相同显示去重、滚出视图的旧显示保留，跨局不串归。
- 新增实际红色目标标记及粘咕黑粒子格子观察和 game.visual 历史。终态等待稳定世界的完整绘制及合格可见源的首次 drawable，不强制渲染、刷新FOV或推进Actor。隐藏源不能导致等待；自然淡出/闪烁不独自让意图过期。
- 为暂停或冻结的渲染源增加纯只读 canProgress 防护，不能自然更新的源不阻塞首显等待；没有把未复现的“普通重建必卡死”当成已确认故障。
- 协议握手公开 build_id、session_id、audit_schema_version；私有快照新增已存在的恢复用 .spdtmp 数据、保存路径被目录占用的事实，未跟随的游戏 symlink 明确标不完整。特殊 IEEE 浮点保留符号、类型及原始NaN位型。
- 验证：既有全套175项通过，暂停源防护追加2项定向通过；中文窗口化日志完整真实回归、红标/粘咕/隐藏源3项及菜单遮挡恢复通过。粘咕首粒子晚于初始终态的真实失败记录保留，修复后以同次操作终态通过验收。
- 改进隔离测试窗口清理：即使受控实例清理失败，也关闭本次额外GUI；新增窗口焦点只读断言证明无焦点时真实投掷及普通GUI/CLI共用实例锁。正式战士窗口不参与测试进程清理。
- 当前视觉源仍是第一组，空提示不代表安全；Tengu、召唤等其余绘制提示继续逐组完善。战士正式通关仍为0/1，实际失误/消耗不回滚。

## CLI.0.3.0

- 审计 schema 4 增加真实 session 生命周期、run/存档槽历史和保存回执表；请求、每次输入、事件、日志、异常关联 session，补齐接收、处理、执行意图和结算时刻。共享记录的时点只生成一次，公私两库一致；旧记录不伪造会话、回执或缺失的历史时间。
- 原生保存回调生成独立回执，关联原始 scope/id；先提交双库再公开。操作结果区分内存执行和本次请求中的保存确认，历史及最初 in_progress 响应不会被后来的保存覆盖。保存后的稳定观察只作 following_snapshot，不宣称它等于保存瞬间或可恢复全部 UI。
- EOF 仅对已证明未执行的 STALE_STATE 重新观察后尝试正常退出；未知结果和强制交互不重放、不自动确认。app.quit 的广告、验证和执行共用同一前置检查，阻塞选择中不再广告不可用退出。
- 修复 LostInventory 时 CLI 可打开原界面禁用物品的差异；公开库存保留灰项并标 available=false，原保留物品仍可正常操作。
- 修复未捕获 Actor 异常及 session 建立后的早期初始化失败被误记为正常/未结束的生命周期记录。实际异常堆栈与回执关联保留内部库，公开仍只给安全类别和保存事实。
- 验证：146 项 JUnit 通过；22 项低频真实交互、真实保存/EOF/重启、11 个实际游戏 JVM 精确强杀、Actor 未捕获异常和包内明确启动失败通过。实际 ARM64 包通过中文路径、多实例锁、原始字节、EOF及末帧、诊断归档和源代码/包内相同的布局重建测试。
- 建立主菜单、普通开局、40 敌人/8 同伴/4 气体的性能基线和 1000 条完整合成审计史，保留原始测量及无损重建证据；未降低同步强度或省略快照。
- 正式通关仍为 0/6。真实战士尝试已推进并正常解锁盗贼，所有失败、未保存进展和消耗损失如实保留；测试夹具及人为护符结算不计通关。

## CLI.0.2.0

- 连续移动加入真实 sprite/Actor 交接屏障和可审计取消；不重复调用游戏行为，不跨数据库事务持有渲染 monitor。休息、移动、取消登记失败与过期/重复取消均已有真实引擎回归。
- 修正切层加载线程的稳定点判断；捕获实际 GUI 输入代数，排除飘字消失对操作意图版本的干扰，同时保留完整 UI 审计。未处理的原生长按保持无动作语义，不补发普通点击。
- 审计 schema 升至 3，保存每条输入的完整原始字节与分帧类型，兼容 LF/CRLF/EOF；非法 UTF-8 明确拒绝，不成对 Unicode 代理项不能成为 ID 或历史目标 ID。旧审计记录标记 legacy-text，不伪造丢失的历史字节。
- 包内 CLI 加入 ARM64 原生启动前置程序，在同一 JVM 启动前确定 profile 与致命错误日志路径；重启归档可读取的诊断文件。内置运行时和 SQLite 不依赖系统另装 Java 或 SQLite。
- 修复欢迎页旧清理逻辑删除空实例锁造成多进程绕锁：锁后写入非空标记，清理只检查游戏保存文件与游戏目录，不触碰审计或无关文件。
- 验证：120 项 JUnit 通过，其中 12 项是生产双库账本的真实子进程 SIGKILL；旧档身份迁移及两种真实保存失败/锁定/重启恢复通过。实际 `.app` 已通过完整基础游玩 smoke、中文应用/数据路径、非法 UTF-8 后恢复、原字节、重复请求、多实例锁、无换行末帧、EOF、诊断归档、ARM64、plist 和本地签名检查。
- 固化 P7 的 188 项真实隔离能力矩阵及补充回归，明确夹具不计通关；正式游戏仍为 0/6。低频交互扩展、真实游戏精确强杀边界、复杂长局性能和六职业通关继续实施。

## CLI.0.1.0

- 新增三个独立控制模块、同 JVM 串行 NDJSON 启动器和 ARM64 `.app` 双入口；开发运行使用冻结 classpath，避免重建 JAR 污染运行中的游戏。
- 实现每局请求 ID 去重、过期状态拒绝、公开历史查询、跨重启身份和明确的保存/胜负通知。
- 实现公开/内部 SQLite 成对事务、原始交换与异常审计、完整声明范围快照、无损压缩/复用和未决操作 UNKNOWN 恢复。
- 接入通用场景/窗口/控件/选物/目标语义，保留原有回调；读取区分玩家知识、地图表象、鉴定、可见环境和实际 UI。
- 增加 Actor handoff、原生休息的串行取消、动画/投射延迟回调完成检查，避免查询触发游戏 getter 副作用或提前报告动作完成。
- 验证：92 项 JUnit 通过；实际包内 CLI 完成开局、正常教程、投掷取消、重复/旧版本拒绝、保存退出、同局重启与双库完整性检查；ARM64/Info.plist/本地签名检查通过。
- 尚未完成：长路径移动主动取消、完整长期/故障/性能验收，以及六个职业的正式通关。专项夹具不计为通关证据。

## CLI.0.0.0

- 建立同 JVM、串行 NDJSON 控制接口的实施契约和验收清单。
- 固定每局请求去重、过期状态拒绝、公开信息过滤和双库完整审计要求。
- 阶段状态按实际验证更新；尚未完成的功能不得视为可用于通关。
