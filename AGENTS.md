# 项目协作约定

这些约定适用于本仓库后续工作；用户的最新明确要求优先。

## 分支

- `master` 保留上游基线，不直接承载本 fork 的开发提交。
- CLI 开发和当前 fork 改动使用 `feature/cli`。用户指定其他功能分支时按其要求执行。
- 创建提交或调整分支前检查当前分支、暂存区和工作区，保留用户已有改动。

## 语言

- 所有新提交的标题和正文都必须使用英文，包括文档、归档、整理、合并和回退提交。
- 本 fork 新增或维护的 README、CHANGELOG 使用中文；代码标识符、命令、路径和专有名称保留必要原文。
- 历史通关笔记和记忆摘录中的旧提交语言约定仅用于复盘；后续工作使用本文件规定的英文提交信息。

## 每次提交的变更日志

- 每次提交都必须实际修改根目录 `CHANGELOG.md`；代码、文档、配置、测试、资料归档和目录整理均不例外。
- 使用中文记录该次改动及必要的验证、兼容性或限制说明，不只添加与改动无关的占位文字。
- 阶段版本沿用 `CLI.主版本.次版本.修订版本`。文档和归档阶段可递增修订版本，并明确其不代表新增 CLI 功能。
- 每个可验收阶段独立提交；检查该提交相对于父提交包含有效的 CHANGELOG 更新。

## 验证与提交范围

- 提交前检查暂存内容，并运行 `git diff --cached --check`。
- 按改动选择必要验证：程序改动检查相应行为；纯文档或归档整理检查链接、文件清单和内容保留情况。
- 保留当前 Git 签名配置，不为绕过失败而关闭签名。
- 公共仓库只收录本项目内容，原始个人 memory、完整会话日志和系统缓存不得混入提交。
- 仅在用户明确要求时推送远程。
